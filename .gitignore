.venv/
__pycache__/
*.db

# Local secrets (SMTP password, JWT_SECRET, agent tokens). Never commit these —
# `.env.example` is the checked-in template.
.env
.env.*
!.env.example
.pytest_cache/
.ruff_cache/
.mypy_cache/
.serena/

# Per-developer Claude Code permission allowlist. `.claude/settings.json` (the
# shared policy, e.g. the .env deny) IS tracked; this one is local noise.
# Previously only ignored via a personal global gitignore, so a fresh clone would
# have committed it.
.claude/settings.local.json
